# SSI extraction pipeline — runtime dependencies.
#
# HARD CONSTRAINT: every package here must resolve for Python 3.14 (this project
# runs 3.14.6). Before adding anything, verify it:
#
#     python -m pip install --dry-run <package>
#
# without --no-deps, so a transitive dependency that lacks a cp314 wheel surfaces
# too. `paddlepaddle` fails this check, which is why PaddleOCR/PP-Structure are
# not used and RapidOCR (the same PP-OCR models via onnxruntime) is.
#
# Versions are lower bounds, matching what was verified on 2026-08-21.

# --- orchestration -----------------------------------------------------------
langgraph>=0.2
langgraph-checkpoint-sqlite>=2.0

# --- model providers ---------------------------------------------------------
openai>=1.40                 # Azure OpenAI adapter (default provider)
anthropic>=0.34              # optional second provider

# --- configuration and validation -------------------------------------------
pydantic>=2.9
pydantic-settings>=2.4
jsonschema>=4.23
pyyaml>=6.0

# --- PDF parsing -------------------------------------------------------------
pymupdf>=1.24
pdfplumber>=0.11

# --- image enhancement and OCR ----------------------------------------------
pillow>=10.3
numpy>=2.0
opencv-python-headless>=4.10
scikit-image>=0.24
rapidocr>=3.0   # PP-OCR detection/recognition models
onnxruntime>=1.19

# --- PII masking -------------------------------------------------------------
presidio-analyzer>=2.2.35
presidio-anonymizer>=2.2.35
# spaCy model is OPTIONAL. Without it, masking falls back to kind-driven rules
# (identifiers still masked; free-text names/addresses tokenised wholesale):
#   python -m spacy download en_core_web_lg

# --- domain validation -------------------------------------------------------
schwifty>=2024.5             # BIC registry lookup (opt-in, Stage 7 only)
pycountry>=24.6              # ISO 3166 / ISO 4217

# --- security and CLI --------------------------------------------------------
cryptography>=43.0
rich>=13.7
